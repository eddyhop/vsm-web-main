version: '3.5'
services:

  vsm-web:
    container_name: vsm-web
    hostname: vsm-web
    image: docker.netlinkz.dev/vsm-web:2023.03.24-02.22.57
    working_dir: /app
    restart: always
    ports:
        - '8080:80'

